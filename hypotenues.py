import math

side_a = float(input("enter the length of the side : "))

side_b = float(input("enter the length of the side : "))

side_c = math.sqrt(pow(side_b, 2) + pow(side_a, 2))

print(f"the hypoteneus is : {side_c}cm")
